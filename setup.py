from setuptools import setup

setup(name='graph_unique_paths',
      version='1.0',
      description='Calculates max unique paths in directed graph with start and end nodes',
      url='',
      author='Aaron Shaver',
      author_email='',
      license='MIT',
      packages=['graph_unique_paths'],
      zip_safe=False)
